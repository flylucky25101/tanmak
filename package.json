{
  "name": "lumenfall",
  "version": "1.0.0",
  "private": true,
  "description": "모바일 우선 세로형 탄막 슈팅 게임",
  "scripts": {
    "build": "node scripts/build.js",
    "build:check": "node scripts/build.js --check",
    "test": "npm run build:check && node build/harness.js && node build/smoke.js"
  },
  "engines": {
    "node": ">=18"
  }
}
